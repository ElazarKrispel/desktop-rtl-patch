# OS / editor
Thumbs.db
.DS_Store
*.swp
*~

# dependencies
node_modules/

# local artifacts / backups
*.bak
*.asar
*.png
tmp/

# ...but the published artwork under assets/ is part of the repo
!assets/*.png

# raw screen captures feeding tools/Compose-Screens.ps1 (never published)
tools/raw/

# release build output (Build-Release.ps1)
dist/

# local media (never publish)
video recordings and screenshots/
